# This workflow is disabled - we deploy via Vercel now
# GitHub Pages doesn't support API routes (requires static export)
# Keeping file to prevent auto-recreation

name: Deploy to GitHub Pages (Disabled)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is disabled. Site deploys via Vercel."
